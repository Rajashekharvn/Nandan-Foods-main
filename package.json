{
    "name": "nandan-foods-monorepo",
    "version": "1.0.0",
    "scripts": {
        "install-client": "cd client && npm install",
        "install-server": "cd server && npm install",
        "build": "cd client && npm install --include=dev && npm run build",
        "start": "cd server && npm install && node server.js"
    },
    "dependencies": {},
    "devDependencies": {}
}